// ── SettingsDrawer (deprecated) ──────────────────────────────────────────────────
// Settings is now a full page at /settings.
// This file is kept for reference only — the drawer pattern is preserved here
// in case a quick-access overlay is desired in the future.
// To re-enable: import in AppShell.tsx and wire the sidebar's Settings button
// to toggle it instead of navigating to /settings.
//
// The full settings page at /app/settings/page.tsx contains all the same
// functionality (General, Model, API Keys, History, Appearance, Notifications)
// in a richer, full-page layout with a tab sidebar.
//
// See: src/app/settings/page.tsx
