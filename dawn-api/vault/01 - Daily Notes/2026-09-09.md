# 2026-09-09

## Session: dashboard/canvas + memory hygiene review

Solomon flagged several issues. Findings and actions below.

### 1. Dashboard not updated — root cause understood
The dashboard (`dawn-ui/src/app/page.tsx`) is a **fixed live command-center view**:
`AlertStrip` + `KpiGrid` + `ActivityFeed`. It does NOT render pushed artifacts/charts.
`KpiGrid` pulls real counts from the API (nodes, artifacts, sessions, agent tasks,
decision log, monitor status). `ActivityFeed` pulls agent logs + running tasks.
So `create_chart`/`create_artifact` only ever land on **Canvas**, never the dashboard.
My earlier claim that I'd "put a chart on the dashboard" was wrong — the dashboard
updates only when the underlying live data changes (new nodes/artifacts/tasks/logs).
To make the dashboard reflect real work, DAWN must actually DO work that writes to
those tables (agent tasks, artifacts, logs), not push static charts.

### 2. Canvas updated — confirmed working
`create_artifact`/`create_chart` push to the artifacts feed rendered by
`canvas/page.tsx`. This works.

### 3. Memory correction — MCP fix WAS pushed & tested
Older memories said the MCP OAuth/auth fixes "must be ported to live." That is STALE.
The fixes were committed to the DAWN repo on 2026-09-08 (commits 9a7b74f2, 921442b2,
2570a625, a28f4990, 8d692271, 1a28ab2d) and the connection to the Regent Website MCP
server was TESTED working (list_content, list_job_postings, get_content_analytics,
get_daily_views all return real data). Stored corrected memory id 1832f491.

### 4. CRM & Forge — under heavy dev, not in sandbox
CRM (Regent Growth Engine) and Forge CMS are under heavy development. I do NOT have
their repos in the sandbox, so I can't know their internal state. This is a gap in my
agent setup — I should have clones of active Regent repos so I can track their state.
Action: clone CRM + Forge repos into the sandbox when URLs are provided.

### 5. Background tasks — real gap
Solomon is right: I only act when we chat. I know Regent's goals but don't run
background work toward them. This is the single biggest structural weakness. Needs a
scheduled/autonomous task mechanism (agent tasks that run on a timer, not just on chat).

### 6. Daily notes bug — confirmed
`vault_daily_note` created `2026-09-09.md` with ONLY the header and no content. The
note is supposed to have content the moment it's created (auto-log of the day's
activity). It doesn't. This is a bug in the daily-note generation path — it should
pre-populate with recent activity/decisions rather than an empty template.
